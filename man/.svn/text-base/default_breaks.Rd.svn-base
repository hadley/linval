\name{default_breaks}
\alias{default_breaks}
\title{Create default breaks for those the user didn't specify...}
\usage{default_breaks(data, bins=5, breaks=list())}
\description{Create default breaks for those the user didn't specify}
\examples{default_breaks(diamonds)
default_breaks(diamonds, bins = 4)}
