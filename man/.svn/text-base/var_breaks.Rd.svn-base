\name{var_breaks}
\alias{var_breaks}
\title{Determins if the variable is of factor type and if not,...}
\usage{var_breaks(x, bins)}
\description{Determins if the variable is of factor type and if not,
create the break values.}
\examples{var_breaks(diamonds)}
